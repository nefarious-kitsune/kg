<table class="basic-table price-table border-all sticky-header">
<thead>
<tr>
  <th class="season-col">Season</th>
  <th class="tier-col"><span
    --has-hint --hint-text="{{TIER A DESC}}">Tier-{{TIER A}}</span
  ></th>
  <th class="tier-col"><span
    --has-hint --hint-text="{{TIER B DESC}}">Tier-{{TIER B}}</span
  ></th>
  <th class="tier-col"><span
    --has-hint --hint-text="{{TIER C DESC}}">Tier-{{TIER C}}</span
  ></th>
  <th class="tier-col"><span
    --has-hint --hint-text="{{TIER D DESC}}">Tier-{{TIER D}}</span
  ></th>
  <th class="tier-col"><span
    --has-hint --hint-text="{{TIER E DESC}}">Tier-{{TIER E}}</span
  ></th>
</tr>
</thead>
{{BODY}}
</tbody>
</table>